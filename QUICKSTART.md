# Тез Қўлланиш (Quick Start)

Лойиҳани махаллий ўрнатиш учун қўйидаги қадамларни амал қилинг:

## 1️⃣ Зависимоталарни ўрнатинг

### Backend (лойиҳа сўзи асосидан)
```bash
pip install -r requirements.txt
```

### Frontend
```bash
cd invest-uz
npm install
cd ..
```

## 2️⃣ Муҳит ўзгарувчиларини ўрнатинг

### Backend - .env файлни яратинг
```bash
cp .env.example .env
```

**МҲИМ: .env файлни таҳрир қилинг ва OpenRouter API калитини қўйинг:**
```
OPENROUTER_API_KEY=sk-or-v1-ххххххххххххххххххххххххх
```

Бепул ақча орхунни https://openrouter.ai/ дан олинг

### Frontend
```bash
cd invest-uz
cp .env.example .env
cd ..
```

(Одатда шунда уста ўринли қиймати бор)

## 3️⃣ Икки сервернипи ишга туширинг

### Терминал 1 - Backend (сўзи асосидан)
```bash
python -m uvicorn main:app --reload
```

Бўлажак:
```
INFO:     Started server process
INFO:     Uvicorn running on http://127.0.0.1:8000
```

### Терминал 2 - Frontend (invest-uz папкасидан)
```bash
cd invest-uz
npm start
```

Браузер `http://localhost:3000` манзилида очилади

## 4️⃣ Иловани ишлатинг

- **Асосий саҳифа** - тавсиф, MVP режа
- **Demo** - таржима ва текст анализ тестлаб кўринг (3 режим)
- **Sign Up** - ҳисоб яратинг
- **Chat** - AI билан гапиринг
- **Профил** - статистика кўринг
- **Юқорида ўнг томон** - тема ўзгартиргич (4 ўзгаруч)

## 🆘 Ишмасса

### "Cannot find module 'dotenv'"
```bash
pip install python-dotenv
```

### "Missing OPENROUTER_API_KEY"
Текширинг:
1. .env файлни тўлдирди
2. Ақча орхун https://openrouter.ai/ дан нусхаланди
3. Backend қайта ишга тушди (`Ctrl+C` ва яна `python -m uvicorn...`)

### CORS хатоси браузер консолида
.env фиқида `ALLOWED_ORIGINS` `http://localhost:3000` ни ўз ичида еқанлигини текширинг

### 8000 ё 3000 портсини эгасини бошқасі
Бошқа иловаларни ёпинг ё бошқа портдан фойдаланинг:
```bash
python -m uvicorn main:app --reload --port 8001
npm start -- --port 3001
```

## 📊 Синар маълумотлари

Биринчи ишга туширишдан сўнг автоматик admin ҳисоб ясалади:
- Username: `admin`
- Password: `admin123`

## ✅ Демога тайёр!

- Frontend `http://localhost:3000` да ишлаяпти
- Backend API `http://localhost:8000` да
- OpenAPI ҳужжатлари `http://localhost:8000/docs` да

Хакатонда омадингиз комил! 🚀
