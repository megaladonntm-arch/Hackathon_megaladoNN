# Быстрый старт (Quick Start)

Следуй этим шагам чтобы запустить проект локально:

## 1️⃣ Установи зависимости

### Backend (из корня проекта)
```bash
pip install -r requirements.txt
```

### Frontend
```bash
cd invest-uz
npm install
cd ..
```

## 2️⃣ Настрой переменные окружения

### Backend - создай .env файл
```bash
cp .env.example .env
```

**ВАЖНО: Отредактируй .env и вставь свой OpenRouter API ключ:**
```
OPENROUTER_API_KEY=sk-or-v1-ххххххххххххххххххххххххх
```

Получи бесплатный ключ на https://openrouter.ai/

### Frontend
```bash
cd invest-uz
cp .env.example .env
cd ..
```

(Обычно там уже правильные значения)

## 3️⃣ Запусти оба сервера

### Терминал 1 - Backend (из корня)
```bash
python -m uvicorn main:app --reload
```

Увидишь:
```
INFO:     Started server process
INFO:     Uvicorn running on http://127.0.0.1:8000
```

### Терминал 2 - Frontend (из papи invest-uz)
```bash
cd invest-uz
npm start
```

Откроется браузер на `http://localhost:3000`

## 4️⃣ Используй приложение

- **Главная страница** - описание, MVP план
- **Demo** - потестируй перевод и анализ текста (3 режима)
- **Sign Up** - создай аккаунт
- **Chat** - поговори с AI
- **Профиль** - посмотри статистику
- **Сверху справа** - переключители тем (4 варианта)

## 🆘 Если что-то не работает

### "Cannot find module 'dotenv'"
```bash
pip install python-dotenv
```

### "Missing OPENROUTER_API_KEY"
Проверь что:
1. Заполнил .env файл
2. Ключ скопирован с https://openrouter.ai/
3. Перезапустил backend (`Ctrl+C` и снова `python -m uvicorn...`)

### CORS ошибка в консоли браузера
Проверь в .env, что `ALLOWED_ORIGINS` содержит `http://localhost:3000`

### Port 8000 или 3000 уже занят
Закрой другие приложения или используй другой порт:
```bash
python -m uvicorn main:app --reload --port 8001
npm start -- --port 3001
```

## 📊 Тестовые данные

После первого запуска автоматически создается admin аккаунт:
- Username: `admin`
- Password: `admin123`

## ✅ Готово к демо!

- Frontend работает на `http://localhost:3000`
- Backend API на `http://localhost:8000`
- OpenAPI docs на `http://localhost:8000/docs`

Удачи на хакатоне! 🚀
